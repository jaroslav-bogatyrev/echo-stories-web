---
import Layout from '../layouts/Layout.astro';
import { siteConfig } from '../lib/config';

const base = import.meta.env.BASE_URL;
---

<Layout 
  title="Contact Echo Stories - Film Production Prague"
  description="Get in touch with Echo Stories. Contact producer Iuliia Mamaeva for film collaborations and project inquiries. Based in Prague, Czech Republic."
>
  <div class="max-w-3xl mx-auto px-6 py-12 md:py-16">
    <h1 class="font-display text-3xl md:text-4xl uppercase mb-12 text-ink">Contact</h1>

    <!-- Producer Profile Section -->
    <div class="mb-12 pb-12 border-b-[3px] border-ink/20">
      <div class="flex flex-col md:flex-row gap-8 items-start">
        <!-- Profile Photo -->
        <div class="flex-shrink-0">
          <div class="w-48 h-48 ink-border overflow-hidden">
            <img
              src={`${base}/profile.jpg`.replace(/\/+/g, '/')}
              alt="Iuliia Mamaeva - Producer"
              class="w-full h-full object-cover"
            />
          </div>
        </div>

        <!-- Profile Info with Contact Details -->
        <div class="flex-1 space-y-6">
          <div>
            <h2 class="font-display text-xl md:text-2xl uppercase mb-2 text-ink">Iuliia Mamaeva</h2>
            <p class="text-base text-muted mb-4">Producer</p>
          </div>

          <div>
            <h3 class="text-sm text-muted uppercase font-bold tracking-wide mb-2">Email</h3>
            <a
              href={`mailto:${siteConfig.email}`}
              class="text-base text-brand-red font-semibold hover:underline transition-colors"
            >
              {siteConfig.email}
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- Company Address and Information -->
    <div class="grid md:grid-cols-2 gap-x-12 gap-y-8">
      <!-- Left Column - Company Info -->
      <div class="ink-border p-5">
        <h3 class="text-sm text-muted uppercase font-bold tracking-wide mb-3 star-accent">Company Information</h3>
        <div class="space-y-1 text-base text-ink/80">
          <p>
            <span class="font-semibold text-ink">Echo Stories</span> – production label run by:
          </p>
          <p class="text-ink/70">Echo Film Production s.r.o.</p>
          <p>IČO: 24107514</p>
          <p>DIČ: CZ24107514</p>
        </div>
      </div>

      <!-- Right Column - Address -->
      <div class="ink-border p-5">
        <h3 class="text-sm text-muted uppercase font-bold tracking-wide mb-3 star-accent">Address</h3>
        <p class="text-base text-ink">Příčná 1892/4, Nové Město</p>
        <p class="text-base text-ink">110 00 Praha 1</p>
        <p class="text-base text-muted mt-1">{siteConfig.location}</p>
      </div>
    </div>
  </div>
</Layout>
